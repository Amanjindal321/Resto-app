/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {                   //change
        ignoreDuringBuilds:true //change
    }                        //change
};

export default nextConfig;

